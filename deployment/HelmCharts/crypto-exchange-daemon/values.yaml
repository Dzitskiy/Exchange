replicaCount: 1
rtsClusters:
  service:
    global: true
    affinity: "local"
  ingress:
    serviceUpstream: true
  replicas:
    enable: false
    single: false
    count:

ingress:
  host: ""

image:
  repo: ""
  tag: ""
  pullPolicy: IfNotPresent

app:
  environment: ""

resources:
  limits:
    cpu: 1.5
    memory: 2Gi
  requests:
    cpu: 0.01
    memory: 200Mi

service:
  port: 80

deployment:
  containerPort: 8080

settings:
  swagger:
    disabled: "false"
  logging:
    minimumLevel: "Information"

messageQueue:
